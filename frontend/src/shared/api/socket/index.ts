export { getSocket, getSocketId } from './socket';
export type { AppSocket } from './socket';
export type {
  ServerToClientEvents,
  ClientToServerEvents,
  BoardChangedPayload,
  JoinBoardPayload,
  JoinBoardAck,
} from './events';
